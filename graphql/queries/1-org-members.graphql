query {
    organization(login:"github") {
        login
        name
        members(first:100) {
            edges {
                node {
                    login
                    location
                }
            }
        }
    }
}