query getRepoIssue {
  repository(owner: "ORGANIZATION_SLUG", name: "REPO_NAME") {
    issues(last: 1) {
      edges {
        node {
          number
          id
          body
        }
      }
    }
  }
}